@customLayout() 
    @slot('main')
    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-2"><span class="text-muted fw-light">Car Categories /</span> Edit Car Category</h4>
{{--  <div class="row mb-4">
    <div class="col-12">
        <button type="button" class="btn btn-primary" data-bs-toggle="modal"
        data-bs-target="#modalCenter">
            <span class="tf-icons bx bx-plus"></span>&nbsp; Create Car Category
        </button>
    </div>
</div>  --}}
    <div class="row">
    <div class="col-12">
        <div class="card">
            <h5 class="card-header"> Edit Car Category</h5>
        <div class="card-body">
            <form action="{{route('admin.car_categories.update', [carCategory.id])}}?_method=PUT" method="POST">
                {{ csrfField() }}
          <div class="row">
            <div class="col mb-3">
              <label for="nameWithTitle" class="form-label">Category Name</label>
              <input
                type="text"
                name="name"
                id="nameWithTitle"
                value="{{carCategory.name}}"
                class="form-control"
                placeholder="Enter Category Name"
                required
              />
            </div>
            </div>
            <div class="row">
            <div class="col mb-3">
                <button type="submit" class="btn btn-success">Update Category</button>
              </div>
          </div>
        </form>
        </div>
          </div>
    </div>
</div>  
      </div>
    @end
@end