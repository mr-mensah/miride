@customLayout()
@slot('main')
<div class="container-xxl flex-grow-1 container-p-y">
  <h4 class="fw-bold py-3 mb-2"><span class="text-muted fw-light">Cars /</span> List of Cars
    ({{cars.length}})</h4>
    <div class="row mb-4">
      <div class="col-12">
          <a href="{{ route('vendor.cars.create')}}" class="btn btn-primary">
              <span class="tf-icons bx bx-plus"></span>&nbsp; Add Car
          </a>
      </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card">
        <h5 class="card-header"></h5>
        <div class="table-responsive text-nowrap">
          <table class="table">
            <thead>
              <tr>
                <th>Car Name</th>
                <th>Category</th>
                <th>Brand</th>
                <th>Price</th>
                <th>Rentals</th>
                <th>Actions</th>  
              </tr>
            </thead>
            <tbody class="table-border-bottom-0">
              @each(car in cars)
              <tr>
                  <td>{{ car.name }}</td>
                <td>{{ car.category.name }}</td>
                <td>{{car.brand.name}}</td>
                <td>{{car.price}}</td>
                <td>{{car.rentals.length}}</td>
              
                {{--  <td><span class="badge bg-label-success me-1">Verified</span></td> --}}
                <td>
                  <div class="dropdown">
                    <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                      <i class="bx bx-dots-vertical-rounded"></i>
                    </button>
                    <div class="dropdown-menu">
                      <a class="dropdown-item" href="{{route('vendor.cars.show', [car.id])}}"><i class="bx bx-show me-1"></i> View</a>
                      <a class="dropdown-item" href="{{route('vendor.cars.edit', [car.id])}}"><i class="bx bx-pencil me-1"></i> Edit</a>
                      <a class="dropdown-item text-danger" onclick="showDeletePrompt('{{route('vendor.cars.destroy', [car.id])}}', 'car')"><i class="bx bx-trash me-1"></i> Delete</a>
                    </div>
                  </div>
              </tr>
              @end
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

</div>

@end
@end