@customLayout()
@slot('main')
<div class="container-xxl flex-grow-1 container-p-y">
  <h4 class="fw-bold py-3 mb-2"><span class="text-muted fw-light">Vendor /</span> Rentals
    ({{rentals.length}})</h4>
   
    <div class="row">
      <div class="col-12">
        <div class="card">
          <h5 class="card-header"></h5>
          <div class="table-responsive text-nowrap">
            <table class="table">
              <thead>
                <tr>
                  <th>Car Name</th>
                  <th>Price</th>
                  <th>Rented By</th>
                  <th>Date Rented</th>
                  <th>Return Date</th>
                  <th>Status</th>
                  <th>Actions</th>  
                </tr>
              </thead>
              <tbody class="table-border-bottom-0">
                @each(rental in rentals)
                <tr>
                    <td>{{ rental.car.name }}</td>
                  <td>{{rental.price}}</td>
                  <td>{{rental.user.fullName}}</td>
                  <td>{{rental.startedAt}}</td>
                  <td>{{rental.endedAt}}</td>
                  <td>{{rental.status}}</td>
                
                  {{--  <td><span class="badge bg-label-success me-1">Verified</span></td> --}}
                  <td>
                    <div class="dropdown">
                      <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                        <i class="bx bx-dots-vertical-rounded"></i>
                      </button>
                      <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{route('user.rentals.show', [rental.id])}}"><i class="bx bx-show me-1"></i> View</a>
                        <a class="dropdown-item" href="{{route('user.rentals.edit', [rental.id])}}"><i class="bx bx-pencil me-1"></i> Edit</a>
                      </div>
                    </div>
                </tr>
                @end
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

</div>
{{--  <div class="modal fade" id="modalCenter" data-bs-backdrop="static" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalCenterTitle">Add Car </h5>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"
        ></button>
      </div>
      <div class="modal-body">
          <form action="{{route('vendor.cars.store')}}" method="POST">
              {{ csrfField() }}
        <div class="row">
          <div class="col mb-3">
            <label for="nameWithTitle" class="form-label">Car  Name</label>
            <input
              type="text"
              name="name"
              id="nameWithTitle"
              class="form-control"
              placeholder="Enter Car  Name"
              required
            />
          </div>
          </div>
          <div class="row">
          <div class="col mb-3">
              <button type="submit" class="btn btn-primary" id="saveCategory">Save Car </button>
            </div>
        </div>
      </form>
      </div>
    </div>
  </div>
</div>  --}}
@end
@end